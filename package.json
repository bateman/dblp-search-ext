{
  "name": "dblp-search-ext",
  "version": "3.8.1",
  "private": true,
  "description": "Cross-browser extension for searching publications on dblp.org",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}
